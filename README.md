this template is based on https://github.com/roland-KA/clean-dhbw-typst-template

with adjustments to TU Ilmenau

see https://github.com/typst/typst for details to get typst running

edit and autocompile on save:

```bash

typst watch main.typ
```

only compile:

```bash

typst compile main.typ
```


